function binary_search(prices, target):
    low = 0
    high = len(prices) - 1
    while low <= high:
        mid = (low + high) // 2
        if prices[mid] == target:
            return True
        elif prices[mid] < target:
            low = mid + 1
        else:
            high = mid - 1
    return False


for each test case i:
    if binary_search(prices, X):
        print("Available")
    else:
        print("Not Available")
